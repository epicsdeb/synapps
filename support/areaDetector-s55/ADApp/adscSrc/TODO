* Add support for ADBase exposure shutter control.
* Add support for creating NDArray data from images written to disk (since
  detcon_lib_th does not provide a way to get the image data directly) and
  calling back any plug-ins (see marCCD and Pilatus drivers as examples of
  this approach).
